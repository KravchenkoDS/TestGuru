<h1>Passing the <%= @passed_test.test.name %> Test</h1>

<% if @passed_test.current_question.nil? %>

  <p>
    There is no available Questions <br>
  </p>

  <%= link_to 'Back to Tests', tests_path %>

<% elsif @passed_test.current_question.answers.count.zero? %>

  <%= render 'header_and_question' %>

  <p>
    There is no available Answers <br>
  </p>

  <%= link_to 'Back to Tests', tests_path %>

<% else %>

  <%= render 'header_and_question' %>

  <%= render 'form' %>

<% end %>